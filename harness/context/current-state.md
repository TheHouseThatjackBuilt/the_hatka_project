# Текущее состояние

Актуально на 2026-09-14. Правила — в [AGENTS.md](../../AGENTS.md), устройство
проекта — в [architecture.md](architecture.md).

## Реализовано

- Визуальная основа редизайна: изолированная тема, 22 базовых и 16 семантических токенов,
  локальный IBM Plex Sans 400/500 с кириллицей, образцы на `?ui=foundations`.
  Основной просмотрщик сохраняет прежнее оформление. Результат и ограничения
  проверки — в [приёмке R1.1](../../docs/viewer-foundations-validation.md).
- R1.2: Button, IconButton, FloatingPanel, StatusBadge и Toggle в
  `src/components/ui/`; варианты, размеры и состояния на том же demo.
  Проверки — в [приёмке R1.2](../../docs/viewer-primitives-validation.md).
- R1.3: SegmentedControl, Select и Tooltip; клавиатура, длинный список,
  всплывающие элементы и cleanup на demo. Все восемь примитивов R1 готовы
  к интеграции; [приёмка R1.3](../../docs/viewer-selection-validation.md).

- Просмотрщик на React Three Fiber: разрез на 1,05 м, полные стены 2,7 м,
  план сверху, вращение, перемещение, масштабирование, вписывание модели,
  мышь и жесты, видимость мебели и названий. Потолок скрыт в просмотрщике.
- Загрузка с обработкой ошибок, скачивание GLB, адаптивный интерфейс,
  ручная камера и demand rendering. Миграция R3F описана в
  [плане](../../docs/r3f-viewer-migration-plan.md) и
  [приёмке](../../docs/r3f-viewer-migration-validation.md).
- Генератор на TypeScript и экспорты JSON/GLB/OBJ/MTL. `model:check` входит
  в сборку; отдельный генератор запускается CLI, браузер загружает готовый JSON.
- Модель полностью пересмотрена по «Итоговая планировка.pdf», лист 09:
  619 деталей, 20 материалов, 9 подписей. Пользователь разрешил измерения
  вектора и выбрал замену прежней кастомной расстановки на PDF.
- Размеры стен, окон, проёмов, пола и мебели согласованы через `plan.ts`.
  Габариты шкафов включают фасады и ручки. Все 64 размерные линии проверяются
  по готовой геометрии. Противоречия и основания — в
  [аудите](../../docs/plan-dimension-audit.md), исходные измерения — в
  [реестре](../../docs/plan-dimensions-audit.json).
- Для текущей квартиры добавлен отдельный эталон. Исторический эталон
  переноса с Python сохранён для совместимости экспортов; правила — в
  [tests/fixtures/README.md](../../tests/fixtures/README.md).
- Кнопка «Размеры»: габариты целых предметов и участков стен, площади и стороны
  кабинета, спальни и двух санузлов, расстояние между контурами объектов на плане
  и между двумя точками поверхности. Наведение, закрепление, списки выбора,
  клавиатура, SVG-линии и адаптивная панель. В каталоге 136 объектов измерения;
  геометрия квартиры сохранена. Подробности и приёмка — в
  [описании реализации](../../docs/measurement-implementation-status.md).

## Ограничения

- Для разработки, генерации экспортов и CI используется Node 24.x. На Node
  22.23.2 воспроизводится падение проверки актуальности `apartment.glb`;
  на Node 24.14.0 те же 47 тестов и сборка проходят. Проверка экспортов остаётся строгой.
  Workflow обновлён целиком: checkout/setup-node v5, configure-pages v6,
  upload-pages-artifact v5 и deploy-pages v5. Запуск обновлённого CI на GitHub
  ещё не выполнялся; локальная проверка от 2026-09-13 успешна.
- PDF содержит проектные размеры, не обмер квартиры. Масштаб штампа 1:60
  противоречит линиям; калибровка по нескольким подписанным размерам даёт
  примерно 1:50. Подписи имеют приоритет, небольшие согласования указаны в аудите.
- Высота потолка 2700 мм сохранена из прежнего условия пользователя.
  Высоты проёмов, мебели и декоративная геометрия остаются схематичными.
- Модель состоит из плоского списка примитивов в мировых координатах.
  ID измеряемых объектов добавлены; перетаскивания, истории и сохранения расстановок нет.
- Измерения относятся к геометрии модели, а не к обмеру квартиры. Высоты мебели
  схематичны. Расстояние между предметами считается на плане без учёта высоты
  и препятствий; точки выбираются на поверхности без привязки к рёбрам.
- Кухня-гостиная считается одним помещением, но её контур ещё не подтверждён.
  Площади холла, прихожей и гардеробной ждут согласования границ.
- `public/preview.png`, `reference/` и исходные отчёты миграций — исторические
  материалы; они не показывают новую планировку.
- Автотесты не заменяют проверку WebGL/DOM и физических жестов.

## Направление работы

Следующее направление — визуальный редизайн просмотрщика. По запросу пользователя
2026-09-14 изучен бриф, выполнена сверка с кодом и зафиксирован
[план с подзадачами и оценкой](../../docs/viewer-redesign-plan.md).
Планирование, браузерное исследование R0.2 и все подзадачи R1 выполнены.
Исходные кадры, замеры и эскиз — в
[отчёте R0.2](../../docs/viewer-redesign-validation.md). При первом открытии
в cut модель занимает около 88% высоты сцены; главные подтверждённые проблемы —
композиция мобильных измерений и пересечение подписей в top на узком экране.
Следующий шаг — R2.1, кадрирование и resize, затем композиция просмотрщика.
Выбор темы/размещения измерений
и бюджета производительности ещё не подтверждён. План учитывает существующие
измерения; отсутствующие undo/redo и перемещение мебели не входят в редизайн,
регулятор высоты среза выделен как отдельное предлагаемое расширение.
Пользователь подтвердил сохранение «Перемещать» как панорамирования камеры.
Перед редизайном устранён конфликт переносов строк Git/Prettier: `.gitattributes`
закрепляет LF для текстовых файлов, общий `format:check` теперь проходит.

Сверка размеров и расстановки с PDF выполнена в согласованном объёме.
Редактор, предметная иерархия и перестройка генератора под предметы остаются
отложенными решением 2026-09-09 в [бэклоге](../../docs/room-rework-backlog.md).
Текущая сверка не возобновляет этот этап. Изменение реальных размеров после
отделки потребует новых обмеров.
Отдельно разрешённый этап измерений реализован для предметов и четырёх однозначных
закрытых комнат. Расширение на оставшиеся зоны требует подтверждения их контуров.

## Проверенное состояние

Приёмка R1.3 от 2026-09-14: typecheck, 49 тестов, build/model:check,
format:check; клавиатура radio/Select, длинный список, Escape, focus/hover
Tooltip, повторное открытие, desktop/mobile и production preview.
Границы проверки — в [отчёте R1.3](../../docs/viewer-selection-validation.md).

Приёмка R1.2 от 2026-09-14: typecheck, 47 тестов, build/model:check и
format:check; размеры 28/32 px, клавиатура, disabled, активный режим кнопки,
Toggle, demo 1440×900 и production 390×844. Объём и ограничения —
в [отчёте R1.2](../../docs/viewer-primitives-validation.md).

Приёмка R1.1 от 2026-09-14: typecheck, 47 тестов, build/model:check и
format:check; demo в браузере 1440×900 и 390×844, локальная загрузка двух
начертаний, tabular-nums, клавиатурный фокус и изоляция от старого UI.
Подробный объём и ограничения — в [отчёте](../../docs/viewer-foundations-validation.md).

Браузерный baseline от 2026-09-14: cut/full/top с измерениями и без на четырёх
размерах окна, переключатели и камера в объёме
[отчёта R0.2](../../docs/viewer-redesign-validation.md). В отдельном dev-замере
1920×1080/DPR 1 на RTX 5080 получено около 239 FPS при тестовом вращении;
в покое — 0 дополнительных кадров за секунду. Это не приёмка редизайна
и не оценка мобильного GPU.

2026-09-14 на Node 24.14.0 прошли `typecheck`, все 47 тестов, `build` с
`model:check` и общий `format:check`. После нормализации CRLF → LF содержимое
кода и экспортов не изменилось; `model:build` выполнен по правилам проекта.
Остаётся предупреждение Vite о viewer chunk 628,23 kB (169,81 kB gzip),
оно не прерывает сборку. Браузерная приёмка в этом проходе не выполнялась,
поведение интерфейса не менялось. Подробности — в
[отчёте проверок перед редизайном](../../docs/viewer-redesign-planning-validation.md).

2026-09-11 прошли `typecheck`, 47 Node-тестов и `build`
с `model:check`. В тестах проверяются все 64 размерные линии, два
дополнительных зазора/проёма, примыкания, контур и отсутствие пересечения
мебели со стенами. Браузерная приёмка и форматирование описаны в
[аудите](../../docs/plan-dimension-audit.md). Исторические результаты проверок
R3F и TypeScript относятся к своим запускам. Экспорты измерений обновлены через
`model:build` 2026-09-10. Новые проверки математики, валидации, выбора и жестов,
браузерная приёмка измерений и оставшиеся замечания форматирования — в
[описании реализации](../../docs/measurement-implementation-status.md).
